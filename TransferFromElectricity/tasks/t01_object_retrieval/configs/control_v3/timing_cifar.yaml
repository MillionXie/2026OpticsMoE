# Timing study only: no held-out test or new performance claim.
base_config: formal_cifar.yaml
epoch_timing: true
validation_only: true
stages:
  - {name: experts, epochs: 2}
  - {name: optics, epochs: 2}
  - {name: joint, epochs: 2}
