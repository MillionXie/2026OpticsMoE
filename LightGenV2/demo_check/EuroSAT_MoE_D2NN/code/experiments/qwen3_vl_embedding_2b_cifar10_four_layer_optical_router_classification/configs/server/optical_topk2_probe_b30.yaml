base_config: optical_topk2_quick.yaml

training:
  epochs: 1

batching:
  optimizer_steps_per_epoch: 1

classification:
  validation_samples_per_class: 1
  test_samples_per_class: 1
  num_workers: 0
  max_train_steps_per_epoch: 1
  early_stopping_patience: 0
  run_final_test: false

output_dir: /root/autodl-tmp/runs/qwen3_vl_cifar10_router_classification/probe_b30_optical_topk2
