base_config: moe_alpha40_fixed_crop_actual.yaml
# Restart from verified 87ad best, not the interrupted last checkpoint.
# Paired 20-epoch budget; no inference architecture or parameter changes.
training:
  student_epochs: 20
  staged:
    polish_start: 16
distillation:
  end_epoch: 20
fixed_crop_distillation:
  end_epoch: 12
output_dir: ../runs/simulation/moe_alpha40_crop_actual_short20_20260912_seed42
