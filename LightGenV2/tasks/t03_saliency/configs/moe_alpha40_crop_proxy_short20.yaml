base_config: moe_alpha40_crop_actual_short20.yaml
# Same initialization, views, ground truth, RNG and schedule as actual.
fixed_crop_distillation:
  mode: proxy
output_dir: ../runs/simulation/moe_alpha40_crop_proxy_short20_20260912_seed42
