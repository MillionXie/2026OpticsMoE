base_config: moe_alpha40_extra_control.yaml
# Training-only MGD-inspired recovery; original 87ad core AND head retained.
# Epochs1..3: generator learns while its student input is detached. GT+mapKD
# still update all original trainable parameters throughout. No inference change.
masked_distillation:
  cache_file: ../runs/simulation/teacher_features_20260910/teacher_train_features.pt
  cache_sha256: 39aae6b13452975186266672e51216e72391112df65b23c453de030033826304
  initial_weight: 1.0
  final_weight: 0.1
  end_epoch: 30
  learning_rate: 0.0003
  mask_probability: 0.5
  generator_warmup_epochs: 3
output_dir: ../runs/simulation/moe_alpha40_masked_kd_seed42
