base_config: moe_alpha40_viewreg_cffn.yaml
# Isolate stronger teacher supervision; same GT loss, augmentation, and optics.
distillation:
  initial_weight: 2.0
  final_weight: 0.6
  end_epoch: 60
output_dir: ../runs/simulation/moe_alpha40_viewreg_cffn_kd2_seed42
