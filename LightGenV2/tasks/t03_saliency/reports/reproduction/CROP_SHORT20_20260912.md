# 2026-09-12：不改推理结构的20轮配对试验

用户授权恢复，目标CC 0.87，不保证达到；原正式0.86204960权重保留。
两组从相同87ad best重新开始，不覆盖9月10日中断的actual run，不假装断点续训。
继承已经通过214项CPU测试的固定裁剪实现；仅缩短训练日程。

| 设置 | 两组共有 |
|---|---|
| 初始化SHA256 | 87ad4db51e3f58f9a41d6df09092439e5a09e81e93f88a3bfe2d3d008fafb29a |
| 数据 | SALICON train2014 10000张；val2014 5000张作为public-test；无独立验证集 |
| 预算 | 20 epochs，seed42；前12轮逐图50%固定裁剪，后8轮原图；第16轮进入低学习率精修 |
| 损失 | 保留GT监督、spatial-CC KD2、电子子空间SAM rho=.05、EMA |
| 光学 | 光router Top2，原478 ROI/224专家/17微米/10cm；alpha>=.4；原DC20–30%训练扰动 |
| 结构 | 冻结Qwen前端、原两支融合、原85412参数头；不加参数、不改Merger |
| 选模 | warmstart/epoch1/每5轮/末轮完整public-test，最高CC；只存best与last |

唯一组间差别：actual使用teacher(cropped RGB)，proxy使用crop(teacher(original RGB))。
原图教师目标、增强随机数、图像/GT、所有学习率均相同。精确RGB/GT变换及缓存校验
沿用[固定裁剪合同](VIEW_REGULARIZATION.md)。仅训练视图增强，测试不做增强或后处理。
公开测试参与选模，不能称独立盲测；最终提升须重载best完整复评，并检查alpha和专家分配。
如果出现NaN、缓存身份不匹配立即停止；若完整测试持续下降，不为凑满预算盲目续训。

从仓库根目录运行，先确认分配的GPU无其他计算进程。每个命令独占一张卡，最多两卡：

```bash
python -m pytest LightGenV2/tasks/t03_saliency/tests -q
CUDA_DEVICE_ORDER=PCI_BUS_ID CUDA_VISIBLE_DEVICES=0 HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 OMP_NUM_THREADS=4 MKL_NUM_THREADS=4 python -u -m LightGenV2.tasks.t03_saliency.run --profile main_dc20 --config LightGenV2/tasks/t03_saliency/configs/moe_alpha40_crop_actual_short20.yaml --phase all
CUDA_DEVICE_ORDER=PCI_BUS_ID CUDA_VISIBLE_DEVICES=1 HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 OMP_NUM_THREADS=4 MKL_NUM_THREADS=4 python -u -m LightGenV2.tasks.t03_saliency.run --profile main_dc20 --config LightGenV2/tasks/t03_saliency/configs/moe_alpha40_crop_proxy_short20.yaml --phase all
```

产物分别位于任务runs/simulation下的
`moe_alpha40_crop_actual_short20_20260912_seed42`、
`moe_alpha40_crop_proxy_short20_20260912_seed42`。
实际源码commit、命令、环境、配置与视图身份由run_manifest.json、environment.json、
resolved配置和fixed_crop_provenance.json记录。启动前须确认目标目录不存在，不覆盖旧结果。

状态：配置已准备，待服务器回归测试和启动核验；没有新的性能结果。
