# 50 epoch 同头 baseline 与交付前核查

本次用户要求重新训练不超过50 epoch。历史 CC=0.88968469 来自100 epoch训练、
epoch80选出的头，不能当成本次50 epoch训练结果。

## 已准备的冻结主干方案

`qwen_aligned_head_50.yaml`：预训练Qwen视觉主干冻结，适配器与读出头从随机初始化训练50 epoch，
不是载入旧100 epoch读出头再额外训练50 epoch，也不是解冻Qwen主干。
已向用户明确该口径后，先开展与历史baseline一致的保守重训。
2026-09-13用户明确确认baseline冻结主干；已完成的50轮重训符合本次要求，不再启动解冻主干版本。

读出头与正式光电模型使用同一个 `SaliencyDensityDecoder(192,224)`，85412参数。
Qwen后接适配器197184参数，合计训练282596参数。冻结主干执行原生24层Vision Transformer；
光电模型不因此增加任何Transformer。全网络参数、预训练知识与历史优化预算并不相同。

```bash
python -m LightGenV2.tasks.t03_saliency.aligned_baseline \
  --config LightGenV2/tasks/t03_saliency/configs/qwen_aligned_head_50.yaml \
  --run-dir LightGenV2/tasks/t03_saliency/runs/simulation/qwen_aligned_head_50_20260912_seed42 \
  --seed 42
```

仅在GPU预算允许时复现；新run不得覆盖历史baseline。本次口径已经用户确认。
学习率明确使用task_training：adapter 1e-4、decoder 3e-4，前5轮冻结adapter更新，
第6–35轮联合训练，第36–50轮低学习率收敛。原始预处理、标签、指标实现不变，
SALICON train10000/public-test5000，epoch1/每5轮/最后测试选最高CC；披露测试选模偏差。
默认只保存best和last，结束后重载best完整评估。DataLoader使用spawn，避免继承父进程CUDA上下文。

## 交付前检查清单

- 头的类、state-dict键/形状与85412参数计数一致；不能仅凭相同输出尺寸声称同头。
- 光router Top2，alpha下界0.4，冻结前端，不加入attention、Transformer或额外推理分支。
- 478 ROI、17微米、10 cm、原来的专家布局及训练直流扰动不变。
- 完整5000张的身份、CC算法及归一化保持一致；不裁选高分测试图，不据真值拟合后处理。
- 训练时教师监督只能访问train缓存；辅助训练模块如有不得意外出现在推理中。
- 检查非有限损失、相位是否更新、alpha、专家选择分布以及训练/测试分化。
- 最多两张GPU，释放自己结束/失败的任务，不终止他人的作业。
- 新best需记录SHA256并独立完整复评；变化很小的单次测试不直接替换正式交付候选。

准备配置不等于训练已经完成；本次结果和运行PID以后续运行记录为准。

## 已启动记录

实际代码 `d8653fa6057f96b52d53259999343ff2117c1526`，225项CPU测试通过，已推送GitHub。
服务器工作树 `.worktrees/t03_baseline50_20260912` 固定在该commit，不能在训练期间checkout。
GPU0 `GPU-afc19890-6209-ee4d-622d-e619da5bd5b2`，PID/PGID410437。
run为上述命令的 `qwen_aligned_head_50_20260912_seed42`，预算50轮。
实际manifest确认冻结主干、随机初始化头、282596训练参数、train10000/test5000。
前5轮仅decoder更新，第6轮起adapter也更新；初期成绩低于已训练100轮的旧baseline是正常的，
不能将随机头第一轮结果当成最终baseline性能。
启动日志暂在 `/tmp/t03_qwen_head50_20260912.log`（与DATA不同文件系统，不能边写边跨盘move），
结束后必须复制回该run的 `console.log`，交付不依赖/tmp。

## 50轮训练完成（2026-09-13）

训练完成50轮，最佳第45轮；重新加载best后完整5000张评估：

|指标|结果|
|---|---:|
|CC|0.8748382971|
|KLD|0.1074505283|
|SIM|0.8297793393|
|NSS|0.9791940771|
|AUC-Judd|0.7722570247|
|MAE|0.0685442622|

best SHA256：`689d086b5a390c399df109d07b73185575cd2245998b7bd427f6ccea5b0fe019`。
第35/40/45/50轮CC为.87391541/.87475148/.87483830/.87404706；后期轻微回落，未继续加轮数。
主训练PID410437及其GPU分配已消失，完整控制台日志已复制到run的`console.log`。
记录来源：该run的`selected_checkpoint_test_evaluation.json`、`metrics/training_history.csv`。

不能称此结果为Qwen主干解冻微调，也不能把旧100轮baseline的.88968469抹掉。
两者差异同时包含训练轮数与学习率日程，不证明更换读出头导致性能下降（头规格没变）。
固定权重独立复评输出到该run的`independent_test_recheck/`；其完成情况以report为准。

### 独立复评已完成：测试batch需固定

两次均加载同一SHA、同一5000张ID清单，未训练或改变图像后处理：

|独立复评|CC（逐图float64）|
|---|---:|
|batch32，`independent_test_recheck/reproduction.json`|0.8747874974|
|原始batch96，`independent_test_recheck_original_batch/reproduction.json`|0.8748382794|

原始batch96的指标实现CC=0.8748382971，与训练完成后的重载评估完全一致。
独立float64实现与该值只差1.77e-8；batch32与96之间约5.08e-5差异可由改变batch复现，
不能把它当成模型改进。今后复现本run固定`--batch-size 96`，并同时保留两个结果。
这只定位到batch相关的数值行为，没有进一步断言具体是哪一个CUDA算子。
完整测试ID序列SHA256：`625dec6bc15b2d737d39bc252cfa0c354de217fec0266dcda568913f4a3496d0`。
两个独立复评PID475243/478271均结束、GPU0释放；未测速度/功耗。

```bash
python -m LightGenV2.tasks.t03_saliency.recheck_aligned --system qwen \
  --config LightGenV2/tasks/t03_saliency/configs/qwen_aligned_head_50.yaml \
  --checkpoint LightGenV2/tasks/t03_saliency/runs/simulation/qwen_aligned_head_50_20260912_seed42/best_checkpoint.pt \
  --run-dir LightGenV2/tasks/t03_saliency/runs/simulation/qwen_head50_user_recheck \
  --batch-size 96
```

最后一个目录须为未存在的新复评目录，不能覆盖已有证据。
