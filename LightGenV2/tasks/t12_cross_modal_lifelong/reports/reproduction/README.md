# T12 复现状态

当前代码实现已建立。正式结果只引用 `runs/simulation` 中完成且通过复算的 run；在服务器
smoke 和首轮训练完成前不填写性能数字。

基线包含两种互补口径：`joint_d2nn` 从第一轮访问全部三项任务（四类模态），衡量离线
联合训练能力；`sequential_d2nn` 使用同一个固定容量 D2NN 按三项任务顺序训练，并沿用
MoE 主阶段的 replay 数量和损失权重，衡量固定容量网络的后向遗忘。两者均无 router、
专家槽位或扩容；任务专用电子头的结构与 MoE 相同。
