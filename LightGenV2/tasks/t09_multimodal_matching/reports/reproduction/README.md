# 复现入口

当前尚无训练结果。技术合同见 [任务README](../../README.md)。

数据准备（从仓库根）：

```bash
python -m LightGenV2.tasks.t09_multimodal_matching.prepare --out /path/to/clevr_attribute_pilot
```

该目录为派生数据和原始下载缓存，禁止提交Git。
