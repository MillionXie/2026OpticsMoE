# T12 runs

Generated feature caches, checkpoints, samples and logs are machine-local and
ignored by Git. Every run must retain its resolved config, run manifest,
training history, best checkpoint and last checkpoint.
