# 图搜图实拍电子读出头适配（2026-09-26）

## 已核对的起点

原权重SHA256 c9926cbaaa1ef066d9657a8028dffc130a33915aa9f392551573dc79894192d0。师弟电脑工程：E:/code/guest/2026OpticsMoE/ABO_I2I_Lab_DVP_8um。

protocol.json含200个SKU，1600 TRAIN/gallery、800 QUERY。runs/abo_i2i_20260925/02_full800已有200个四图batch，六层完整实拍QUERY，原R@1=0.21625。图库仍是1600图仿真特征；不得称为全实拍图库测试。

目前未发现独立TRAIN六层实拍缓存。不得直接对800 QUERY更新梯度，再称为独立测试。

## 首轮适配方案

固定seed=20260926，从每个SKU的8张TRAIN选4张实拍，共800张；每SKU3张适配、1张验证，共600/200。需要六层约4800张CCD，不修改原QUERY或其CCD。正式采集前再次确认光路可用、GUI关闭。

仅适配最后电子读出头，冻结Qwen前端、光学router、相位、残差和alpha。先缓存最后读出头之前的实拍特征。查询侧适配头用于对齐固定仿真图库；图库原特征不随适配头更新，需在报告明确这一域适配口径。训练/验证图片本身存在于图库中时，检索计算必须排除相同sample_id，避免自匹配。核查图片SHA重复、标签/索引映射和mask不变。

首轮10 epoch，从原头初始化，使用同SKU多正例检索损失和原输出锚定；按600/200划分的验证R@1选best，同分MRR。选定后在原800 QUERY/1600图库评估一次。保存best、last、history和报告，不按TEST挑样本或轮次。若使用额外adapter或改动头结构，需另行说明，不静默添加。

预计采集需约1～2小时，具体取决于该工程逐batch切相位和小相机速度；特征缓存/微调另计，启动冒烟后提供实际估计。目前仅完成数据核查与方案，未启动TRAIN采集或微调。
