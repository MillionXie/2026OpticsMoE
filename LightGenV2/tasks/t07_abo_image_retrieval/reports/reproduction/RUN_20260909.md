# ABO 图搜图：2026-09-09 本轮证据

## 固定大模型重跑：已完成

源码 `8492ed41`，run=`frozen_qwen_20260909`。
输出根目录：`/DATA/DATA1/guest3/2026OpticsMoE/LightGenV2/tasks/t07_abo_image_retrieval/runs/simulation/`。
冻结 Qwen3-VL-Embedding-2B，未训练 backbone、LoRA 或附加读出头。
离线 snapshot=`9f2f7e710d6d81056aa5c0a4f04764fec6bb7bda`。
在 guest3 服务器运行，只测性能；本轮不报告速度/功耗。

| 预处理/输出 | Hit@1 | Hit@5 | Hit@10 | mAP@10 |
|---|---:|---:|---:|---:|
| 原始长宽比，2048D（主 baseline） | 0.95208333 | 0.98750000 | 0.99791667 | 0.83253323 |
| 原始长宽比，64D | 0.93958333 | 0.98541667 | 1.00000000 | 0.79561516 |
| 光学同尺寸中心裁切，2048D | 0.93750000 | 0.97500000 | 0.99375000 | 0.78069965 |
| 光学同尺寸中心裁切，64D | 0.92916667 | 0.96875000 | 0.99375000 | 0.76326571 |

原始输入由 Qwen processor 保留长宽比、限定像素预算，不应简单称作固定224×224。
square 版本与光学使用同一 `GroceryRetrievalDataset(augment=False)` 224×224 bicubic 中心裁切。
MRL 64D 为前64维截取后重新 L2 归一化，再构建每商品12视角均值图库。
所有版本都是480查询×120商品中心；同类12个商品为相关，不先按类别路由候选库。
本轮 native 2048D 精确复现历史 Hit@1，不能与 T08 图搜文分数混用。

证据 SHA256：

- `baseline_report.json`：`3dc60220d354c8a25a05b1c91be011343fef58ed6d11e24c8c6212818811e3c4`
- `features.pt`：`0f0178dc25dc02727412d3632bf1e28aa463f204b696ec5a4822955b27c2ba18`
- 数据 ZIP：`c8f0f79cbd8ceb3c42162092136254a1f44f6d13b3329e0e3b1c5dfc458c1485`

本轮逐字节哈希检查全部2400图像，跨 train/val/test 的相同文件内容重复组为0；不代表排除了所有视觉近重复。
仍保留商品划分120/40/40，validation 480图未参与训练/选模。
完整命令见本目录 [README](README.md)，每个 run 的 `run_manifest.json` 保存实际 argv 和环境。

## 光学适配：40 epoch 正式训练已启动

源码 `bdfcb9e9`，profile=`configs/optical_top2_dc20.yaml`。
run=`optical_top2_dc20_20260909`，实际使用 RTX4090 UUID `GPU-e8837b85-d55b-8e81-aaa5-ec1ac326932d`。
源代码工作树为 `/DATA/DATA1/guest3/2026OpticsMoE/.worktrees/t07_abo_20260909`，
其 task/runs 链接到上面的统一输出目录；不要在服务器旧根工作树直接覆盖源码。

已完成2个训练 step的小检查：前向、反向、完整480-query评估、EMA best恢复、同权重去光复评、相位图和对照图均成功。
小检查不作为正式性能。所有四组特征相位和两个光 Router 的参数改变量非零；best/last之外没有周期相位模型快照。

正式参数：每epoch60个batch，每batch4类×3图；40epoch，5epoch一次完整test选择，EMA0.99。
损失为类别监督对比+0.3教师蒸馏+均衡/相位约束；类别标签仅用于train loss。
测试只排序图像embedding与训练商品图像中心，没有title查询或类别预筛选。
test参与选模，因此最终数值必须标注test-selected，不能当独立无偏测试。

光学成绩、alpha、去光差值及专家使用率等待正式run完成；不要把小检查的数字填入论文总表。
运行结束看 `final_report.json`、`comparison.png`、`best_phase_overview.png`，过程看 `history.json` 与 `status.json`。
