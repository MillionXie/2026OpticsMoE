base_config: spatial_electronic_rf_vision_s667.yaml
output_dir: ../../runs/lgvq_spatial_electronic_rf_both_aug_s669
random_seed: 669

# Mild cached-view augmentation is training-only; evaluation remains the
# canonical uniformly sampled four frames.
data:
  training_view_probabilities: [0.80, 0.10, 0.10]

training:
  trainable_scope: appended_electronic_only
  learning_rate: 0.00005
