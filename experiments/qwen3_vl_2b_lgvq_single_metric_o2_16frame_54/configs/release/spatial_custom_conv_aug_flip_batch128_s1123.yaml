base_config: spatial_custom_conv_aug_flip_s1019.yaml
output_dir: ../../runs/lgvq_spatial_custom_conv_aug_flip_batch128_s1123
random_seed: 1123

# Larger MOS-stratified batches improve the training-only estimates of Pearson,
# differentiable Spearman and pairwise ordering.  Inference is unchanged.
training:
  initialization_checkpoint: ../../../../LightGenV2/tasks/t06_video_quality_assessment/runs/simulation/spatial_paired_views_s991/best_paired_view_deploy.pt
  epochs: 45
  batch_size: 128
  learning_rate: 0.0000015
  readout_learning_rate_factor: 0.20
  ema_decay: 0.99
  test_interval_steps: 9
  horizontal_flip_probability: 0.50

