base_config: spatial_residual_warm_seed42.yaml
output_dir: ../../runs/lgvq_spatial_qwenfront_o2_4f109_dc20_pyramid_r200_rank

model:
  spatial_readout_mode: spatial_pyramid_residual
  spatial_residual_max: 2.00

training:
  trainable_scope: residual_only
  epochs: 80
  batch_size: 32
  learning_rate: 0.00001

loss:
  ranking_weight: 1.00
  correlation_weight: 3.00
  soft_target_weight: 0.00
