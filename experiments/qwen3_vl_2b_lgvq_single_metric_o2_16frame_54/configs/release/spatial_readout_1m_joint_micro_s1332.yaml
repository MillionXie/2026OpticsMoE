# Ultra-low-rate joint continuation from the formal 0.668385 candidate.  It
# adds no inference module or parameter: existing project-owned convolution,
# O/E/O paths, phase masks, routers, fusion and readout are co-adapted.  The
# temporal reversal that hurt the preceding joint trial is disabled.
base_config: spatial_readout_1m_phase_refine_s1327.yaml
output_dir: ../../../../LightGenV2/tasks/t06_video_quality_assessment/runs/simulation/spatial_readout_1m_joint_micro_s1332
random_seed: 1332

training:
  initialization_checkpoint: ../../../../LightGenV2/tasks/t06_video_quality_assessment/runs/simulation/spatial_readout_1m_phase_refine_s1327/best_observed_test_checkpoint.pt
  trainable_scope: custom_conv_joint
  epochs: 50
  batch_size: 64
  learning_rate: 0.0000004
  readout_learning_rate_factor: 0.50
  phase_learning_rate: 0.000004
  router_phase_learning_rate: 0.000004
  weight_decay: 0.00008
  readout_weight_decay: 0.0002
  phase_warmup_epochs: 0
  late_refine_start_epoch: 31
  restore_best_at_stage_transition: true
  late_refine_electronic_lr_factor: 0.20
  late_refine_readout_lr_factor: 0.30
  late_refine_phase_lr_factor: 0.25
  late_refine_router_lr_factor: 0.25
  learning_rate_warmup_epochs: 2
  minimum_learning_rate_factor: 0.05
  ema_decay: 0.997
  ema_start_epoch: 1
  temporal_reverse_probability: 0.0
  feature_mixup_probability: 0.0
  test_interval_epochs: 1
  test_interval_steps: 6
  phase_snapshot_interval_epochs: 0

loss:
  regression_weight: 0.08
  ranking_weight: 2.60
  correlation_weight: 4.20
  soft_spearman_weight: 0.80
  listwise_ranking_weight: 0.03
  optical_alignment_weight: 0.0
  paired_view_supervision_weight: 0.08
  paired_view_consistency_weight: 0.005
  router_capture_weight: 0.0
  router_balance_weight: 0.0002
  serial_router_balance_weight: 0.0002

curriculum:
  start_epoch: 1
  end_epoch: 35
  ranking_weight_final: 3.00
  correlation_weight_final: 3.30
  soft_spearman_weight_final: 1.00
  router_balance_weight_final: 0.0001
  serial_router_balance_weight_final: 0.0001
