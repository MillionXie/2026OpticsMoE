base_config: spatial_residual_warm_seed42.yaml

output_dir: ../../runs/lgvq_spatial_qwenfront_o2_4f109_dc20_residual_rank_r030_seed42

model:
  spatial_residual_max: 0.30

training:
  trainable_scope: residual_only
  epochs: 100
  batch_size: 64
  learning_rate: 0.00008

loss:
  ranking_weight: 0.80
  correlation_weight: 1.50
  soft_target_weight: 0.00
