base_config: spatial_trainonly_fixed20_step2_rank_s460.yaml
output_dir: ../../runs/lgvq_spatial_trainonly_fixed20_routerdiv_s464
random_seed: 464

data:
  training_view_probabilities: [1.0, 0.0, 0.0]

training:
  initialization_checkpoint: ../../runs/lgvq_spatial_trainonly_interp_s463/best_observed_test_checkpoint.pt
  trainable_scope: serial_router_and_readout
  epochs: 5
  batch_size: 64
  learning_rate: 0.000001
  router_phase_learning_rate: 0.001
  readout_learning_rate_factor: 0.35
  minimum_learning_rate_factor: 0.20
  phase_warmup_epochs: 0
  restore_best_at_stage_transition: false
  ema_decay: 0.95
  test_interval_steps: 4
  phase_snapshot_interval_epochs: 5

loss:
  ranking_weight: 1.00
  correlation_weight: 2.00
  soft_spearman_weight: 0.20
  soft_rank_temperature: 0.15
  optical_alignment_weight: 0.0
  router_balance_weight: 0.0
  router_importance_weight: 0.0
  serial_router_balance_weight: 0.05
  serial_router_importance_weight: 0.02
  serial_router_diversity_weight: 0.02
  router_capture_weight: 0.02

