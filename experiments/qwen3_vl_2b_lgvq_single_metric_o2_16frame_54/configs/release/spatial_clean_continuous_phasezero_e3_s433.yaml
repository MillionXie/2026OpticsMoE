base_config: spatial_clean_continuous_phasezero_s432.yaml
output_dir: ../../runs/lgvq_spatial_clean_continuous_phasezero_e3_s433
random_seed: 433

# One additional sequential residual-convolution block is added inside the
# existing electronic path. This remains a two-branch E/O inference graph.
model:
  electronic_route_depth: 3

training:
  learning_rate: 0.00005
  readout_learning_rate_factor: 0.40
