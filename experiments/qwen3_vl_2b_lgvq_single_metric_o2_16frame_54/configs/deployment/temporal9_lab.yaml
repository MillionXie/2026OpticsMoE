base_config: ../release/temporal9_compact_zero_order_crop.yaml

# Self-contained paths used by the Temporal-9 laboratory ZIP.  Keep these
# relative paths unchanged after extracting the archive.
output_dir: ../../lab_runs/temporal9

data:
  dataset_root: null
  manifest: ../../deployment/data/lgvq_train2250_test558.csv
  vision_cache: ../../deployment/data/qwen3vl_front_9f_49x1024_quality14.pt
  language_cache: ../../deployment/data/qwen3vl_front_temporal_prompt_2048.pt
  quality_feature_cache: null
  raw_frame_cache: null
  vgg_feature_cache: null
  training_soft_targets: ../../deployment/data/training_only_teacher_predictions.pt

training:
  # The laboratory bridge always receives the packaged checkpoint explicitly.
  # Clear the source-server warm-start path inherited from the release config.
  initialization_checkpoint: null

initialization:
  qwen_model_path: null
  reset_serial_router_phase: false
