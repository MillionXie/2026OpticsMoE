base_config: nonshared_grocery10_finetune.yaml

dataset:
  train_limit_per_sku: 2
  test_limit_per_sku: 1

batching:
  teacher_batch_size: 4
  pk_skus_per_batch: 10
  pk_images_per_sku: 2
  inference_batch_size: 1
  num_workers: 0
  optimizer_steps_per_epoch: 1

training:
  epochs: 1
  log_interval_batches: 1

evaluation:
  checkpoint: ema_last

teacher_cache_path: ../../cache/smoke_nonshared_optimization_teacher.pt
output_dir: ../../runs/smoke/nonshared_optimization
