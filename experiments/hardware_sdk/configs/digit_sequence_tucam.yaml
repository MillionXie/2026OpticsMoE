output_dir: ../artifacts/demos/tucam_digit_sequence
settle_delay_ms: 200
confirm_before_start: true

digits:
  # synthetic 不依赖 Torch/torchvision，适合实验室控制电脑。
  source: synthetic
  slm_size_wh: [1920, 1080]
  active_size_wh: [956, 956]
  digit_size_px: 700
  foreground_level: 255
  background_level: 0

devices:
  amplitude_slm:
    driver: holoeye
    sdk_path: "%HEDS_3_2_PYTHON_MODULES%"
    binary_folder: null
    expected_resolution_wh: [1920, 1080]
    minimum_refresh_hz: 59.0
    preload: true
    wait_until_visible: true

  camera:
    driver: tucam
    sdk_path: ../vendor_sdk/camera_tucam_mosaic
    camera_index: 0
    timeout_ms: 10000
    output_extension: .npy
    config_file: null
    auto_exposure: false
    exposure_us: 5000.0
    analog_gain: null
    anti_flicker_hz: null
    resolution_mode: null
    # 必须复制正式采集配置中的同一组 [left, top, width, height]。
    require_device_roi: true
    device_roi_xywh: null
    saved_frame_size_wh: null
    saved_frame_resize_mode: none
    warmup_frames: 3
    discard_frames_after_display: 1
