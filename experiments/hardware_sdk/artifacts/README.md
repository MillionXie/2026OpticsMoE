# Generated artifacts

- `calibration/masks/`: background、coarse、fine、曝光响应和 ROI 验证 BMP。
- `calibration/results/`: background、标定矩阵、残差和响应曲线。
- `demos/`: 相机/SLM 独立自检输出。
- `logs/`: 正式采集 manifest 和解析后的设备信息。

运行产物默认不提交 Git；可由配置与命令稳定复现。
