numpy>=1.26,<3
Pillow>=10
PyYAML>=6
scipy>=1.11
matplotlib>=3.8
transformers>=4.57,<5
accelerate>=1.1
safetensors>=0.4
pytest>=8

# Install torch/torchvision for the laboratory GPU separately. Vendor PCIe SLM
# and TUCam drivers/runtimes are system software and are not installed by pip.
