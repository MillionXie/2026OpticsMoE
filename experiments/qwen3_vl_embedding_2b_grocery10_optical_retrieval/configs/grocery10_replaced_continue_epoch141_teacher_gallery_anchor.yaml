base_config: grocery10_replaced_continue_epoch141_augmented_relational_kd.yaml

# Anchor augmented Student queries to the frozen Teacher embeddings of the
# standard gallery images. The deployed metric remains Student query versus
# Student gallery; this extra loss is training-only and adds no parameters.
training:
  lambda_teacher_gallery: 0.25

output_dir: ../runs/qwen3_vl_embedding_2b_grocery10_replaced_continue_epoch141_teacher_gallery_anchor
