# 2026-09-12 联合部署记录

机器目录：`E:\code\guest\2026OpticsMoE\ABO_Lab_SHS_8um`。
只测试振幅 SDK 与 SHS 相机；相位由操作者保持全黑，无自动相位切换。

## 软件与 GPU 已通过

- `.venv_gpu`: Python 3.12 x64，torch 2.8.0+cu126、torchvision 0.23.0+cu126、
  transformers 4.57.3、accelerate 1.10.1、safetensors 0.6.2。
- RTX4060：图像和文本各一个完整仿真前向，输出均为 64 维、范数 1、全部有限。
  模型自动选择 bfloat16；峰值 allocated 874.90 MiB / reserved 890 MiB。
  两个样本耗时 1.986 s 包含冷启动影响，**不是推理速度基准**。
- 第一阶段 4 张振幅 BMP：`results/cuda_model_check_20260912`；1920×1080，
  保留原 17 μm→8 μm 物理宽度插值规则，不反振幅灰度。
- 固定 checkpoint SHA256：`a2aa9a93028410dfb780df7d320a9be65d07b6e7ec87ede08b53c4bddcbaf7af`。
- 资源 ZIP 817,678,734 字节，SHA256：
  `f4af613bc5f2e88062f16fdc2116a4d884123d98c6f6526d881988a047629da1`。
  逐文件校验后导入，包含 checkpoint、runtime、原始查询图和前端参数。
  未改变原始权重数值，不需要加载完整 Qwen Transformer 权重。

## 光路测试：尚不能确认输入调制

Holoeye 原生 SDK 3.2.1 + 匹配 Python wrapper（API5）成功打开，等待 Visible 成功。
在登录桌面执行；SSH 无桌面进程不适合显示 SLM。
Windows 仅发现一块 HE PLUTO-2.1，操作者随后已确认它是输入振幅而非相位。

SHS 100 fps，换图额外等待 200 ms，再丢弃 6 帧；每次首次开流预热 2 s。
首轮 50/200/1000 μs × 4 张，共 12 帧；中间灰度复核 1000/3500 μs × 5 档，共 10 帧。
全部短测结束相机设置恢复成功，不持续占用相机。

|3500 μs 输入灰度|全幅平均灰度|
|---|---:|
|0|17.79997|
|64|17.81893|
|128|17.80762|
|192|17.83961|
|255|17.79042|

该组 p99=50、最大值152～156、饱和比例0。能看到光场边框，但五档均值几乎不变。
因此不是仅仅“0与255透过率恰好相同”的证据；尚需检查面板对应、输入显示与光束位置。
**不能确定新的 LUT、最佳曝光或最短等待；暂不自动增加曝光、不运行正式几万张采集。**

操作者确认设备后补测全屏 0/64/128/192/255（3500 μs、每档等待500 ms，共5帧）：
均值依次17.68525、17.73126、17.69422、17.73071、17.71446，p99=49～50，
最大156～159，饱和比例0。仍未发现明确调制；排除了仅仅光束落在中心方块外的解释，
不能仅靠增加等待继续盲试。原图及记录：`results/joint_full_20260912`。
尚需现场核对光束经过输入SLM、偏振片/检偏器及面板实际显示是否改变；这不是已确定的故障原因。
测试结束停止换图，保持振幅黑图、相位不动，相机恢复运行前设置。

原图/逐帧时间戳：`results/joint_scout_20260912`、`results/joint_gray_20260912`。
后续 `timing` 模式先用独立重复帧检查左右输入差异；无法区分就中止，避免静态错误图取得虚假高 PCC。
没有硬触发，丢 6 帧只是保守缓存策略，不是已认证的曝光同步。

## 正式保存与待办

默认每次保留一张 canonical 478×478 PNG（压缩级别1）及必要 JSON/索引；不保存冗余 TIFF/全幅 raw。
诊断模式仍保存少量原图，方便追查。新 CCD 四角尚未填写，`geometry_confirmed=false` 保持阻止盲采。

下一步：现场核对光束/振幅调制 → 短测可见换图响应 → 选择不饱和曝光 → 交替图时序验证
→ 重新标定四个 CCD 逻辑角 → 每层手动切相位，4 查询试跑六阶段 → 评估。
目前没有新光路的 ABO 实测准确率，不能把软件烟雾测试当作复现 80.583%。
