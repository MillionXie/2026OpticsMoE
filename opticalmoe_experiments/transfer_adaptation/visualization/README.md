# Transfer Visualization

These scripts read a transfer run directory and regenerate figures for:

- target training curves
- target prompt swap
- source/target expert usage
- target/source prompt similarity

The training script writes the same figures automatically at the end of each run.

