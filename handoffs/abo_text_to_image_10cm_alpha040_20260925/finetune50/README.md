# 50 epoch 微调：验证选模，TEST 仅评一次

2026-09-26 按用户要求从原始仿真 checkpoint 重新初始化，不从先前 200 epoch 权重续训。使用已采集的 800 张 TRAIN 的冻结实测特征，按固定种子 20260926 每商品 6 张适配、2 张验证，共 600/200；不重新占用硬件。保持学习率 0.0002、温度 0.07、anchor 权重 0.1 及原双向检索目标。可训练部分仍是共享的最终 LN(384)+Linear(384,64) 读出头，作用于图片与标题；不是固定标题分支的新对照。

训练期间不加载 TEST 缓存。按验证 Hit@1 选最优，同分依次比较验证 MRR/MAP；选定后才加载原 TEST 的 2,400 张图和 100 个标题缓存，使用 FP32 读出头评估一次。没有评估最后一轮之外的其他候选 TEST，也没有额外 BF16 全前向；报告中的 `test_evaluation_count=1` 指本次新运行，不表示这些 TEST 在项目历史上未被评估过。

- 最佳 epoch：50；验证 Hit@1=0.95。
- TEST Hit@1=0.96、Hit@5=0.99、Hit@10=1.00、MRR=0.9764285087585449、MAP=0.866929292678833。
- 最佳权重：`best_readout_checkpoint.pt`；SHA256 `331a12e43c17d62bbc9fec9d7757180988c870aea0da79cb540bfcdf01c2cf86`。下载后已核对。
- `history.csv` 包含每个 epoch 的训练/验证结果，不包含逐轮 TEST。

结果仍属于同一固定商品集合上的实测适配，不据此保证新商品/新光路泛化。先前异常高结果的进一步审计问题没有因为这次只跑 50 epoch 就自动消失。
