# ABO图搜图：高速相机全实拍（2026-09-26）

采用0.83125仿真版本，原主体SHA256 c9926cbaaa1ef066d9657a8028dffc130a33915aa9f392551573dc79894192d0。本轮不微调。1600图库与800查询全部重新通过六层真实光路，共14400张ROI PNG。旧的实拍查询/仿真图库成绩0.21625仅作历史混域对照。

## 独立方向及动态范围检查

ROI来自师弟电脑E:/code/guest/20260926.txt：TL[586,147]、TR[1379,159]、BR[1369,946]、BL[573,932]。真实相机1920×1080 Mono8。正式配置恢复此前高速相机配置：400μs、Gain_X4、换图等待240ms、buffer_count=4、启动预热2秒；请求100fps，实际帧时序以SDK时间戳为准。

振幅非对称标记确认相机统一上下翻转flip_v，标记PCC=0.89993，重复PCC=0.99620。相位局部光栅测得TL→BR、TR→BL、BL→TR、BR→TL，因此全部六层相位统一hv翻转。灰度255-g沿用原硬件配置；本次未完成独立电压-相位曲线测量，不能称灰度相位线性已校准。取消旧的逐层仿真PCC筛方向，固定物理方向。

前期Gain_X1全白诊断及2000μs网络短测属于历史诊断，不是当前正式配置。恢复400μs/Gain_X4后，首4张六层共24张审计通过：各阶段最低p99为12～15、全局最大像素47、饱和比例0。该配置用于实际网络输入，不批准用于全白诊断；正式饱和门槛1%超限自动报错停止。

## 时序依据

SDK启动预热2秒；复用ABO_Lab_SHS_8um/slm_camera.py原始Controller：每张BMP单独预载并显示，每次SLM换图后持续取帧排空旧队列，再fresh()丢缓冲数+2帧后使用下一帧。相位仅在换层时下发并等待0.8秒，随后排空相机帧；该等待不等于每张振幅输入的240ms等待。

原始Controller在240ms下30轮/60张交替标记测试未发现旧图，最低重复PCC=0.9968423254792633，见shs_previous240_report.json。前期新封装的200～400ms异常和700ms测试保留为历史证据，不可据此声称原始采集器必须700ms；具体异常机制尚未完全隔离。有限测试不是永不失败保证。

## MNIST短测

使用既有10cm原生8μm B_RECOMMENDED_native8_best.bmp（hv翻转、255-g），固定四张数字，固定探测框[162,162,221,221]、[257,162,316,221]、[162,257,221,316]、[257,257,316,316]。真实ROI四区积分分别判对0/1/2/3，4/4，仅证明小样本光路流程可用，不能作为完整数据集准确率。未按这四张标签选择方向或探测框。

## 操作与结果路径

师弟电脑工程仍在E:/code/guest/2026OpticsMoE/ABO_I2I_Lab_DVP_8um，入口run_shs_physical2400_20260926.cmd。先加--max-batches 1运行首4张六层短测；审计后不加该参数继续，自动跳过已完成batch。

唯一正式目录：runs/abo_i2i_20260926/shs_physical2400_w240_e400_x4。
同级日志shs_physical2400_w240_e400_x4.log；目录内progress.json记录已完成输入数（2400，不是查询数），ccd分六层保存，batch_results每4张保存，最终features.pt与report.json给出全实拍查询→全实拍图库结果。原始全传感器帧仅用于诊断，不在正式每帧另存TIFF。旧shs_physical2400_e2000目录保留隔离，不与新配置混用。正式任务ABO_I2I_SHSFull2400_20260926已去掉--max-batches并启动，后续以动态progress为准。

本地本目录保存配置、验证报告及部分实拍PNG。代码同步GitHub分支codex/t08-physical-test-20260926；最终是否采完以正式progress/report为准，不以本说明静态文字判断。
